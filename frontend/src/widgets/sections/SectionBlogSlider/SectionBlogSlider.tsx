import { BlogSlide } from "../../../shared/types/typesBlogSlider";

import "./SectionBlogSlider.scss";

const SectionBlogSlider: React.FC<{ slides: BlogSlide[] }> = ({ slides }) => {
  return (
    <>
      <div>SectionBlogSlider</div>
    </>
  );
};

export default SectionBlogSlider;
